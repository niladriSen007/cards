
server:
  port: 9000
spring:
  application:
    name: cards
  datasource:
    url: jdbc:postgresql://localhost:5432/jpa-microservice-cards
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true